# 1 Test methods in PyTest

# To display the Test menthod Run icon,
# 1. Make sure you have these extensions installed:
    # Python (by Microsoft)
    # Pylance (recommended for Python)
    # Pytest (optional but helpful)
# 2. Enable testing:
    # Open Command Palette (Ctrl + Shift + P or Cmd + Shift + P on Mac)
    # Type: Python: Configure Tests
    # Choose pytest
    # Choose the folder (e.g. src/PyTest)
    # VS Code will detect test functions starting with test_


def test_validation():
    print("Validation method started..")